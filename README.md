# 🖥️ Ассемблер и Интерпретатор для Учебной Виртуальной Машины (УВМ)

![Python](https://img.shields.io/badge/Python-3.8%2B-blue?logo=python&logoColor=white)
![Assembly](https://img.shields.io/badge/Language-YAML-orange)
![Build](https://img.shields.io/badge/Status-Completed-brightgreen)

Проект представляет собой реализацию **ассемблера** и **интерпретатора** для учебной виртуальной машины. Комплекс позволяет писать программы на человекочитаемом языке (YAML), транслировать их в бинарный код и исполнять с пошаговым изменением состояния памяти.

---

## 🚀 Установка и Подготовка

Для работы необходим Python версии 3.8 или выше.


1. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

---

## 📂 История разработки и Запуск по этапам

Проект разрабатывался в 5 этапов. Ниже приведено описание каждого этапа и команды для проверки функционала.

### 🔹 Этап 1: Ассемблер и Промежуточное представление
**Цель:** Создать CLI-приложение, разбирающее текстовое описание команд (YAML) и формирующее структуру программы.
**Что сделано:** Реализован парсер YAML и битовые операции для упаковки команд.

**Запуск теста:**
```bash
python assembler.py test_program.yaml output.bin --log
```

---

### 🔹 Этап 2: Генерация машинного кода
**Цель:** Реализовать полноценную трансляцию в бинарный файл (`.bin`).
**Что сделано:** Ассемблер теперь сохраняет результат в файл и подсчитывает количество скомпилированных инструкций.

**Запуск:**
```bash
python assembler.py test_program.yaml output.bin --log
```
---

### 🔹 Этап 3: Интерпретатор и работа с памятью
**Цель:** Создать виртуальную машину, умеющую читать бинарный файл, выполнять команды `READ_MEM`, `WRITE_MEM` и `LOAD_CONST`.
**Что сделано:** Реализован цикл выборки инструкций и модель памяти (массив). Добавлен механизм дампа памяти в XML.

**Тестовая задача:** Копирование массива (числа 100, 200, 300) из ячеек 0-2 в ячейки 10-12.

**Запуск:**
```bash
# 1. Ассемблируем
python assembler.py copy_test.yaml copy_test.bin

# 2. Запускаем (проверяем диапазон памяти 0-15)
python interpreter.py copy_test.bin res_copy.xml 0:15
```
> *Ожидаемый результат:* В файле `res_copy.xml` значения в ячейках 10, 11, 12 совпадают с ячейками 0, 1, 2.

---

### 🔹 Этап 4: Арифметико-логическое устройство (АЛУ)
**Цель:** Реализовать вычислительную операцию `BSWAP` (побитовый переворот).
**Что сделано:** В интерпретатор добавлена логика `bswap()`, которая читает значение из памяти, переворачивает порядок байтов и кладет результат в аккумулятор.

**Тестовая задача:** Загрузка числа, его переворот и сохранение результата.

**Запуск:**
```bash
# 1. Ассемблируем
python assembler.py bswap_test.yaml bswap_test.bin

# 2. Запускаем
python interpreter.py bswap_test.bin res_bswap.xml 0:1
```

---

### 🔹 Этап 5: Итоговая тестовая задача
**Цель:** Решение комплексной задачи с использованием всех возможностей УВМ.
**Задача:** Выполнить поэлементно команду `bswap()` над вектором длины 6. Вектор исходных данных лежит по адресам 0-5. Результат записать в адреса 10-15.

**Запуск:**
```bash
# 1. Сборка программы вектора
python assembler.py test_vector.yaml test_vector.bin

# 2. Исполнение (диапазон 0-15 покажет и источник, и результат)
python interpreter.py test_vector.bin res_vector.xml 0:15
```

---

## 🧪 Описание тестовых программ

### 1. `copy_test.yaml` (Копирование памяти)
**Суть:** Демонстрация работы с адресацией.
*   Инициализация: `Mem[0]=100`, `Mem[1]=200`, `Mem[2]=300`.
*   Логика: Чтение происходит по формуле `Addr = ACC + Offset`.
*   Чтобы прочитать данные точно по смещению, перед `READ_MEM` мы обнуляем аккумулятор (`LOAD_CONST 0`).

### 2. `test_vector.yaml` (Векторный BSWAP)
**Суть:** Обработка массива данных.
*   В памяти по адресам `0-5` лежат исходные числа.
*   Программа последовательно читает каждое число, применяет операцию `BSWAP` (переворот байтов) и записывает результат в новую область памяти (`10-15`).
*   Пример: Число `258` (`0x00000102`) превращается в `33619968` (`0x02010000`).

### 3. `test_copy_offset.yaml` (Тест смещения)
**Суть:** Проверка сложения аккумулятора и смещения.
*   Записываем значение по адресу `50`.
*   Загружаем в аккумулятор `20`.
*   Делаем `READ_MEM` со смещением `30`.
*   Машина читает адрес `20 + 30 = 50`.

---

## ⚙️ Спецификация команд (Instruction Set)

Размер команды зависит от Opcode.

| Команда | Opcode (A) | Биты | Описание | Размер |
| :--- | :---: | :--- | :--- | :---: |
| **LOAD_CONST** | 6 | 0-2: Op, 3-11: Const | `ACC = Const` | 2 байта |
| **READ_MEM** | 2 | 0-2: Op, 3-8: Offset | `ACC = Mem[ACC + Offset]` | 2 байта |
| **WRITE_MEM** | 5 | 0-2: Op, 3-28: Addr | `Mem[Addr] = ACC` | 4 байта |
| **BSWAP** | 7 | 0-2: Op, 3-28: Addr | `ACC = bswap(Mem[Addr])` | 4 байта |

---
